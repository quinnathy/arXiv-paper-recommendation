[pytest]
markers =
    live: tests that hit real Google Scholar (require network)
    slow: tests that require loading the SPECTER2 embedding model
    embedding: tests that require real embedding model/artifacts
    diagnostic: tests that print algorithm diagnostics
